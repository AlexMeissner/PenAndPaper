* TokenCreationDto has monsterId and characterId & there is a TokenType enum. The enum should be part of the DTO instead.
* Move Dice related stuff from Map to Dice control
* Event Handler must be registered in Loaded, because if they are registered in ctor they will bot be deregistered when the user control never was loaded (visible)