<Page x:Class="Client.Pages.AdminView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Client.Pages"
      xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Name="AdminViewPage"
      Title="AdminView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Margin="4" Content="Abbrechen" Command="{Binding ElementName=AdminViewPage, Path=ViewModel.CancelCommand}" Style="{StaticResource WarningEmpty}"/>
            <Button Grid.Column="1" Margin="4" Content="Speichern" Command="{Binding ElementName=AdminViewPage, Path=ViewModel.SaveCommand}" Style="{StaticResource SuccessEmpty}"/>
        </Grid>
    </Grid>
</Page>
